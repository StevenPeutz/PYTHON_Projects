Practice scripts.

- Python linkscraper: Script for listing all links on a given (input) webpage. (using BS4, works with TLS)
- Custom linkscraper: scraping all links for a given URL (input) without depending on Beautiful Soup.

